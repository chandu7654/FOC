#include <stdio.h>

int main() {
   int N, i, sum = 0, sign = 1;

   printf("Enter the value of N: ");
   scanf("%d", &N);

   for (i = 1; i <= N; i++) {
      sum += sign * i;
      sign = -sign;
   }

   printf("The sum of the series is %d\n", sum);

   return 0;
}
